import Navbar from "../../components/Navbar";
import SwapCard from "./SwapCard";

export default function Swap() {
  return (
    <section className="h-screen w-screen bg-purple">
      <Navbar />
      <SwapCard />
    </section>
  );
}
